Other policies might respond directly. Imagine a guardrails system that, instead of rejecting a request, simply returns a canned "this appears to violate our content policy" response.

Or a semantic cache, where similar requests result in an immediate return of a stored response to avoid spending resources on inference altogether.

...
